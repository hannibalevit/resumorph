Create an ATS-optimized tailored resume in English.

Preserve exactly: candidate name in candidateName; location, contacts, and all URLs in contactInfo; every company name (exact casing), all employment dates, education facts.

Editing scope:
- headline and summary: rewrite for the target role using resume facts
- skills: reorder by job relevance
- Most recent role bullets: primary surface — rewrite to match requirements
- Second most recent bullets: only if most recent alone is insufficient
- Older positions: remove if clearly irrelevant; never remove the most recent role

Rules: mirror job keywords where truthful | missing requirements → notes.missingRequirements only | no invented metrics | no em dashes (use hyphen) | no AI buzzwords (leverage, synergy, spearheaded, passionate, robust, cutting-edge, etc.) | vary bullet openings | human-sounding prose | zero spelling or grammar errors

Schema:
$tailored_resume_schema

Job:
$job_context_json

Resume:
$base_resume
